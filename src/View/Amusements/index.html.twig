{% extends 'layout.html.twig' %}

{% block stylesheet %}
<link rel="stylesheet" href="assets/css/amusements.css"> {% endblock %}

{% block title %}{{ parent() }} - Attractions{% endblock %}


{% block content %}

<main class="container-fluid amusement-home pt-5">

    
    <h1 class="bg-primary section-title mx-auto mb-5 px-3 rounded-4">Attractions</h1>
    {% for amusement in amusements %}
        <div class="container rounded-4 bg-secondary my-4">
            <div class="row amusement-card  ">
                <div class="col-lg-9 d-flex flex-column justify-content-between {% if loop.index is even %} order-lg-2 {% endif %}">
                    <h2 class="text-center mt-3"> {{amusement.name}} </h2>
                    <p class="mb-4 text-md-center {% if loop.index is even %} text-lg-end {% else %} text-lg-start{% endif %} "> {{amusement.description}}
                    </p>

                </div>
                <div class="col-lg-3 {% if loop.index is even %} order-lg-1 {% endif %}">
                    <img class="img-size rounded-4" src="{% if amusement.image is empty %}assets/images/1200x900_placeholder.png{% else %}{{constant('APP_UPLOAD_PATH') ~ amusement.image}}{% endif %}"  alt="Nom de l'attraction">
                {# {{ dump(constant('APP_UPLOAD_PATH') ~ amusement.image) }} #}
                </div>
            </div>
        </div>  
    {% endfor %}
</main>
{% endblock %}